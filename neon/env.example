# Copy to a local .env (gitignored) when you are ready to count/load.
# Do not put real secrets in this file.

# Supabase (service_role, not the anon/publishable key)
SUPABASE_URL=https://ukwftzmtdgszrvrygroj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Neon FamilyBudget
# Direct URL for schema apply / data load. Pooled URL is fine for DATABASE_URL on Vercel.
NEON_DATABASE_URL=
DATABASE_URL=

# Shared secret checked by /api/snapshot (Vercel env + later in frontend config)
SNAPSHOT_SECRET=
SNAPSHOT_CORS_ORIGIN=*
SNAPSHOT_API_URL=/api/snapshot
ADMIN_API_URL=/api/admin
ADMIN_EMAIL=
ADMIN_PASSWORD=
